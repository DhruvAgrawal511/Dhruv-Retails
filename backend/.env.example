# Environment Variables Template
# Copy this file to `.env` and fill in your actual values
# NEVER commit the `.env` file with real secrets!

# === DATABASE ===
# PostgreSQL connection string
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?sslmode=require
DATABASE_URL="postgresql://shopify_user:password@dpg-xxx.xxx.render.com/shopify_fde_db?sslmode=require"

# === REDIS ===
# Redis connection string (managed on Render)
# Format: rediss://default:[password]@[host]:[port]
REDIS_URL="rediss://default:password@xxx-keyvalue.render.com:6379"

# === SHOPIFY CREDENTIALS ===
# Your Shopify store domain (without https:// or trailing slash)
SHOPIFY_STORE_DOMAIN="your-store.myshopify.com"

# Shopify Admin API access token (obtained from Shopify App)
# Requires: read_products, read_customers, read_orders scopes
SHOPIFY_ACCESS_TOKEN="shpat_xxxxxxxxxxxxxxxxxxxx"

# Shopify Admin API version (use latest)
SHOPIFY_API_VERSION="2025-10"

# Shopify webhook secret (for HMAC verification)
# Generate from Shopify App settings
SHOPIFY_WEBHOOK_SECRET="webhook_secret_xxxxxxxxxxxx"

# === AUTHENTICATION ===
# JWT secret key (use a strong random string, min 32 chars)
# Generate: openssl rand -base64 32
JWT_SECRET="your_super_secure_random_string_here_min_32_chars"

# JWT token expiry (formats: "7d", "24h", "2592000")
JWT_EXPIRES_IN="7d"

# === SERVER ===
# Port for backend server
PORT="4000"

# === OPTIONAL ===
# Node environment (development, production, test)
NODE_ENV="development"

# Log level (debug, info, warn, error)
LOG_LEVEL="info"

# === NOTES ===
# 1. Replace all placeholder values with your actual credentials
# 2. Keep this file secure - never commit to version control
# 3. For local development, use local PostgreSQL and Redis (or Docker)
# 4. For production, use managed services (Render, AWS, etc.)
# 5. Rotate secrets regularly for security
# 6. Use different secrets for dev/staging/production environments
